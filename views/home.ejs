<%- include("partials/header") -%>
  <!-- layout partials -->

  <h1>home</h1>

  <p>
    <%= homeInformation %>
  </p>

  <% posts.forEach(function(post){ %>
    <h1>
      <%= post.title %>
    </h1>
    <p>
      <%= post.content.substring(0, 99) + "..." %><a href="/posts/<%= post._id %>">Read More</a>
    </p>
    <% }); %>

      <%- include("partials/footer") -%>
        <!-- layout partials -->